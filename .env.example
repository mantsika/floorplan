# Local Express dev — create at https://aistudio.google.com/apikey (starts with AIzaSy)
GEMINI_API_KEY="AIzaSy..."

# Cloudflare Pages build — your Workers API URL after deploy:api
VITE_API_URL="https://floorplan-api.your-subdomain.workers.dev"

# Worker secret (never commit): wrangler secret put GEMINI_API_KEY
